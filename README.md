# Skald Vault

Skald Vault is a sovereign multi-rail Bitcoin wallet application for advanced users who keep their own keys, choose their own infrastructure, and need explicit custody, privacy, fee, and recovery boundaries.

Current status: initial Kotlin Multiplatform / Compose Multiplatform scaffold with Phase 1 descriptor-native on-chain domain modeling, non-secret Bitcoin backend profile settings with a unified address input, simulated backend configuration validation, non-secret descriptor wallet profile metadata, non-operational coin-control/PSBT draft planning, fail-closed secure-storage and secure metadata persistence boundaries, an app-controlled encrypted local vault design, a pinned vault crypto dependency probe with candidate-level dependency review, a disabled Skald-owned vault crypto provider boundary, a disabled provider-selection boundary, a provider-level KAT strategy contract for deterministic vectors plus randomized AEAD behavioral checks, a still-disabled provider executable KAT decision gate, a still-disabled provider executable KAT prerequisite audit, a still-disabled test-only executable provider KAT scope decision, a still-disabled test-only provider KAT executor contract, a still-disabled test-only provider KAT vector catalog, a still-disabled test-only provider KAT executor readiness gate, a still-disabled test-only provider KAT source-set confinement boundary, a still-disabled test-only provider identity decision, a still-disabled test-only provider identity isolation guard, a still-disabled test-only provider synthetic identity namespace contract, a still-disabled test-only provider identity source-set confinement boundary, a still-disabled test-only provider identity implementation decision gate, a still-disabled test-only provider identity implementation prerequisite audit, a still-disabled test-only provider identity implementation scope decision, a still-disabled test-only provider identity implementation contract, a still-disabled test-only provider identity implementation readiness gate, a still-disabled test-only provider identity implementation runtime linkage guard, still-disabled test-only provider identity implementation promotion blockers, still-disabled test-only provider identity implementation source-guard coverage, a still-disabled test-only provider identity implementation redaction guard, a still-disabled test-only provider identity implementation admission gate, a still-disabled test-only provider identity implementation plan, a common-test-only inert test-only provider identity marker, a common-test-only inert test-only provider identity inventory, a common-test-only inert test-only provider identity profile, a common-test-only inert test-only provider identity profile validation report, a common-test-only inert test-only provider identity reachability proof, a common-test-only inert test-only provider identity capability matrix, a common-test-only inert test-only provider identity KAT fixture scope, a common-test-only inert test-only provider identity KAT fixture catalog, a common-test-only inert test-only provider identity KAT fixture validation report, a common-test-only test-only provider identity KAT public-vector admission gate, a common-test-only public non-secret test-only provider identity KAT vector fixture, a common-test-only public non-secret test-only provider identity KAT vector validation report, a vault container/manifest/storage/stale-record contract with model-only platform storage boundary, platform storage-root, platform root settings, safe path-construction, symlink/traversal, permission/ownership, durability-capability, durability fail-closed, warning-only rejection, atomic write, crash-recovery, interruption-test, and storage failure evidence, still-disabled in-memory vault container and manifest parser/writer building blocks, a still-disabled local manifest-relative stale-record decision policy, a still-disabled in-memory storage atomicity/crash simulator for fixed non-secret byte-array fixtures, a still-disabled storage namespace/path policy building block for safe identifier validation and deterministic relative segment encoding, a still-disabled logical storage layout plan for deterministic rootless relative artifact segment lists, a still-disabled path-containment planner for reviewed root tokens and root-token-bound relative planned locations, a still-disabled Linux root-resolution evidence policy for caller-supplied static root evidence, a still-disabled platform root resolver boundary for injected Android/Linux root evidence snapshots, a still-disabled platform path-construction boundary for root-evidence-bound logical artifact locations, a still-disabled storage safety preflight boundary for planned artifact-location and future safety-gate evidence, a disabled vault storage service facade for future storage operations that always returns disabled/fail-closed results, a still-disabled vault persistence readiness gate that composes provider/root/path/storage/secure-storage evidence into a blocked decision, a still-disabled vault lock/session lifecycle boundary that models locked/unlock-requested/unlock-blocked/session/forced-lock evidence without accepting passphrases or holding keys, a still-disabled vault redaction/leakage boundary for safe-output classification, a still-disabled vault passphrase policy boundary for future input/normalization/encoding/retry/throttle/lockout policy evidence, a still-disabled vault clear/wipe strategy boundary for future clearance requirements and JVM-zeroization limitation evidence, a still-disabled vault migration/corruption boundary for future container/version/manifest/storage-index/record failure classification and fail-closed action evidence, a stale-record/rollback manifest contract, a test-only provider KAT harness, a still-disabled integrated provider KAT harness, a metadata-only still-disabled provider facade, a still-disabled Argon2id calibration policy/candidate-selection/memory-failure/no-downgrade building block plus non-final candidate parameter policy, manual Android calibration evidence-capture protocol, Android compatibility/entropy policy, and runtime randomness/provider check modeling with test-only Android and Linux/JVM availability probes, a pinned BDK adapter packaging spike, a local `bitcoind` regtest harness, a desktop-test-only local Electrum-compatible regtest indexer harness boundary, a resolved BDK JVM/Linux binding decision, a desktop-test-only BDK regtest wallet validation boundary, a desktop-test-only BDK regtest/signet address derivation validation boundary, receive-address state/reuse policy modeling, Skald-owned backend observation/UTXO state boundaries, a production backend adapter/endpoint normalization boundary, a disabled production sync service facade with Nodes/Recovery/Privacy status surfaces, and a desktop-test-only BDK Electrum regtest UTXO scan validation path that completes when explicitly run with a local `electrs` binary. This repository does not yet implement real wallet functionality or vault persistence. Do not use it with real funds.

## Targets

- Android APK
- Linux desktop app
- Linux `.deb` package

## Build Commands

The intended Gradle tasks are:

```bash
./gradlew build
./gradlew :composeApp:allTests
./gradlew :composeApp:assembleDebug
./gradlew :composeApp:run
./gradlew :composeApp:packageDeb
```

The local regtest smoke harness is opt-in and requires local Bitcoin Core or Bitcoin Knots-compatible `bitcoind` and `bitcoin-cli` binaries:

```bash
SKALD_RUN_REGTEST_INTEGRATION=1 ./gradlew :composeApp:desktopTest --tests '*Regtest*'
```

The seed-backed BDK regtest wallet validation boundary is also opt-in and desktop-test-only:

```bash
SKALD_RUN_BDK_REGTEST_WALLET_VALIDATION=1 ./gradlew :composeApp:desktopTest --tests '*Bdk*' --rerun-tasks
```

The test-only BDK regtest/signet address derivation validation boundary is opt-in and desktop-test-only:

```bash
SKALD_RUN_BDK_REGTEST_ADDRESS_DERIVATION=1 ./gradlew :composeApp:desktopTest --tests '*AddressDerivation*' --rerun-tasks
```

The test-only BDK regtest UTXO scan validation boundary is opt-in and desktop-test-only. Without the local Electrum harness opt-in, it verifies the safe blocked state requiring a local indexed scan backend:

```bash
SKALD_RUN_BDK_REGTEST_UTXO_SCAN=1 ./gradlew :composeApp:desktopTest --tests '*Utxo*' --rerun-tasks
```

To run the desktop-test-only BDK Electrum scan adapter through a local regtest indexer, both opt-ins and local `bitcoind`, `bitcoin-cli`, and `electrs` binaries are required:

```bash
SKALD_RUN_LOCAL_ELECTRUM_REGTEST=1 SKALD_RUN_BDK_REGTEST_UTXO_SCAN=1 ./gradlew :composeApp:desktopTest --tests '*Utxo*' --rerun-tasks
```

The local Electrum-compatible regtest indexer harness is opt-in and desktop-test-only. It expects a local `electrs` binary plus `bitcoind` and `bitcoin-cli`; if `electrs` is missing it reports unavailable rather than using a public backend:

```bash
SKALD_RUN_LOCAL_ELECTRUM_REGTEST=1 ./gradlew :composeApp:desktopTest --tests '*Electrum*' --rerun-tasks
```

Linux packaging requires a JDK that includes `jpackage`. If Gradle selects an Android Studio JBR without `jpackage`, run:

```bash
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew :composeApp:packageDeb
```

The `packageDeb` task passes jpackage a build-local temp directory under `composeApp/build/compose/tmp/` so Linux packaging does not depend on free space in system `/tmp`.

If the Gradle wrapper has not been generated in a fresh checkout, use a local Gradle installation to run:

```bash
gradle wrapper --gradle-version 8.11.1
```

## Security Status

- No real Bitcoin keys, seeds, descriptors, transactions, signatures, Lightning payments, Cashu proofs, Payjoin payloads, Nostr private key parsing, or wallet credentials are implemented.
- Visible balances are static demo/testnet placeholders.
- Mainnet operations are unavailable in the scaffold.
- Development networks are regtest, signet, testnet, and testnet4.
- A seed phrase alone cannot restore every rail; Recovery Center modeling is first-class even in the scaffold.
- Descriptor wallet profiles are non-secret metadata only. The app can save planned profile records, but it does not store descriptor text, xpubs, xprvs, private keys, seeds, nsecs, PSBTs, transaction data, or wallet funds.
- Descriptor/key-management implementation is still absent. The future integration design is documented in [`docs/DESCRIPTOR_KEY_MANAGEMENT_DESIGN.md`](docs/DESCRIPTOR_KEY_MANAGEMENT_DESIGN.md).
- A BDK dependency/adapter packaging spike exists with pinned `org.bitcoindevkit:bdk-android:2.3.0` and `org.bitcoindevkit:bdk-jvm:2.3.0` platform dependencies behind Skald-owned adapter models. It is documented in [`docs/BDK_INTEGRATION_SPIKE.md`](docs/BDK_INTEGRATION_SPIKE.md). It does not enable production wallet creation, key generation, descriptors, app receive addresses, backend sync, PSBTs, signing, broadcasting, persistence, or mainnet.
- The BDK JVM/Linux binding decision is documented in [`docs/BDK_JVM_LINUX_BINDING_DECISION.md`](docs/BDK_JVM_LINUX_BINDING_DECISION.md). The accepted `2.3.0` JVM artifact contains a Linux x86_64 native binding; the previously pinned `2.3.1` artifact was rejected for Linux desktop wallet validation.
- A desktop-test-only seed-backed BDK regtest wallet validation boundary exists and is documented in [`docs/BDK_REGTEST_WALLET_VALIDATION.md`](docs/BDK_REGTEST_WALLET_VALIDATION.md). It completes on the current Linux desktop target through an opt-in, redacted, test-only path and does not expose or persist wallet material.
- A desktop-test-only BDK regtest/signet address derivation validation boundary exists and is documented in [`docs/BDK_REGTEST_ADDRESS_DERIVATION.md`](docs/BDK_REGTEST_ADDRESS_DERIVATION.md). It derives runtime-generated test addresses only under explicit opt-in and does not add app receive UI, address index persistence, backend sync, signing, broadcasting, production wallet storage, or mainnet.
- Receive-address state and reuse-prevention policy modeling exists and is documented in [`docs/RECEIVE_ADDRESS_POLICY.md`](docs/RECEIVE_ADDRESS_POLICY.md). It distinguishes displayed/reserved addresses from backend-observed used addresses, but production receive address generation, BDK-derived app addresses, address index persistence, backend observation, and real receive UI remain disabled.
- Skald-owned backend observation and UTXO state boundaries exist and are documented in [`docs/BACKEND_OBSERVATION_STATE.md`](docs/BACKEND_OBSERVATION_STATE.md). They model backend trust, address-used transitions, observed UTXO lifecycle, and coin-control-required spend readiness without enabling production sync, production UTXO persistence, app UTXO display, signing, broadcasting, public backend defaults, or mainnet.
- A production backend adapter and endpoint normalization boundary exists and is documented in [`docs/PRODUCTION_BACKEND_ADAPTER_BOUNDARY.md`](docs/PRODUCTION_BACKEND_ADAPTER_BOUNDARY.md). It defines Skald-owned adapter request/result types and one-field address parsing for IPv4, IPv6, DNS, and `.onion` metadata while rejecting credentials/userinfo. Nodes/backend settings now use that unified address field while keeping scheme/TLS/transport/port metadata explicit and preserving backward-compatible non-secret host/port storage. The only production adapter remains disabled/fail-closed; no production backend clients, sync, UTXO scan, public defaults, signing, broadcasting, or mainnet are enabled.
- A disabled production sync service facade exists and is documented in [`docs/PRODUCTION_SYNC_SERVICE_BOUNDARY.md`](docs/PRODUCTION_SYNC_SERVICE_BOUNDARY.md). It composes endpoint policy, backend adapter, receive-address policy, secure-storage capability, secure metadata persistence capability, and backend observation models into a fail-closed preflight/result boundary. Nodes now shows a read-only preflight/status card for selected backend and descriptor metadata. Recovery Center and the Privacy Analyzer also surface disabled sync, backend observation, receive-address, secure-storage, secure metadata, and persistence blockers through read-only status cards documented in [`docs/RECOVERY_PRIVACY_SYNC_STATUS.md`](docs/RECOVERY_PRIVACY_SYNC_STATUS.md). These surfaces do not expose working sync actions, connect to backends, sync wallets, persist observations, derive production addresses, sign, broadcast, or enable mainnet.
- A disabled secure metadata persistence boundary exists and is documented in [`docs/SECURE_METADATA_BOUNDARY.md`](docs/SECURE_METADATA_BOUNDARY.md). It classifies address index state, receive-address lifecycle state, observed address usage, observed UTXOs, labels, transaction notes, backend observation history, endpoint metadata, identity-linkage metadata, Privacy Analyzer metadata, and Recovery metadata as sensitive wallet metadata that requires app-controlled encrypted vault storage. The disabled repository rejects reads, writes, listing, and deletes; it does not use settings storage, files, OS keyrings, BDK persistence, or plaintext serialization.
- A still-disabled vault lock/session lifecycle boundary exists as model-only evidence for future locked, unlock-requested, unlock-blocked, active-session-unavailable, session-expired, background-lock, close-lock, error-lock, provider-change-lock, storage-readiness-change-lock, platform-security-change-lock, mainnet-block, and forced-locked states. It consumes typed readiness/provider/storage/secure-storage evidence and lifecycle events only; it does not accept or store passphrases or PINs, derive or hold keys, implement memory wipe/zeroization, biometrics, Android Keystore, OS keyrings, password managers, unlock UI, session persistence, filesystem checks, file reads/writes, Settings persistence, vault unlock, vault persistence, provider selection, production provider use, or mainnet.
- A still-disabled vault redaction/leakage boundary exists as model-only evidence for future value-kind classification, output-target policy, redaction decisions, forbidden classes, allowed public evidence classes, and source-guard material classes. It accepts typed value-kind requests only; it does not accept raw secrets, passphrases, key material, raw paths, payloads, wallet labels, transaction notes, credentials, byte arrays, or backend handles. It does not hash or fingerprint secrets, log anything, add crash reporting, add analytics, add support export, add runtime diagnostics, persist diagnostic output, display secrets, implement unlock UI, implement provider execution, implement storage, enable vault unlock, enable vault persistence, enable provider selection, or approve mainnet. Public non-wallet cryptographic vectors remain allowed only in scoped docs/tests/source-guard contexts; wallet, UTXO, and sync paths continue to reject hardcoded address, txid, and secret-looking fixtures.
- A still-disabled vault passphrase policy boundary exists as model-only evidence for future passphrase input policy, normalization/encoding policy identifiers, retry/throttle/lockout requirements, redaction requirements, clear/wipe review, and unlock prerequisites. It does not accept raw passphrases, PINs, biometrics, key material, hashes, fingerprints, files, paths, settings values, or backend handles. It does not store passphrases, normalize or encode real input, hash or fingerprint passphrases, run Argon2id/KDF/HKDF/HMAC/AEAD, implement retry/throttling or lockout, add passphrase or unlock UI, implement memory wipe/zeroization, add Android Keystore, OS keyrings, password managers, vault unlock, vault persistence, provider selection, production provider use, or mainnet.
- A still-disabled vault clear/wipe strategy boundary exists as model-only evidence for future sensitive value classes, lifecycle triggers, clear/wipe requirements, strategy classes, and limitations. It accepts typed policy requests and typed evidence only; it does not accept raw sensitive values, passphrases, PINs, keys, byte arrays, char arrays, buffers, paths, provider handles, storage handles, Settings values, or backend handles. It does not clear real memory, zero real memory, prove JVM zeroization, use native memory, implement provider clear calls, implement storage clear calls, implement session invalidation, add passphrase or unlock UI, add Android Keystore, OS keyrings, password managers, vault unlock, vault persistence, provider selection, production provider use, or mainnet.
- A still-disabled vault migration/corruption handling boundary exists as model-only evidence for future container/version/manifest/storage-index/record failure classification, stale-record and rollback-suspicion review, partial-write handling, recovery decision classes, redacted failure reporting, and fail-closed action requirements. It accepts typed policy requests and typed evidence only; it does not accept raw persisted bytes, manifest bytes, storage-index bytes, record bytes, ciphertext, plaintext, nonce/tag bytes, header-commitment bytes, keys, passphrases, paths, provider handles, storage handles, Settings values, or backend handles. It does not parse real storage, read files, write files, run migration or dry-run migration, repair storage, quarantine records, recover records, verify AEAD tags, decrypt records, verify real header commitments, rewrite manifests or storage indexes, prove rollback resistance, prove crash recovery, enable vault unlock, enable vault persistence, enable provider selection, approve production provider use, or approve mainnet.
- A still-disabled vault creation authorization boundary exists as model-only evidence for future creation operation kinds, purposes, initializer classes, required gates, blockers, warnings, disabled capabilities, and redacted policy tokens. Current creation authorization is blocked/fail-closed. It does not accept passphrases, PINs, or biometrics; normalize or encode passphrases; generate salts, nonces, keys, container ids, record ids, or metadata ids; run Argon2id/KDF/HKDF/HMAC/AEAD; call provider operations; create headers, header commitments, containers, manifests, storage indexes, records, secure metadata, wrapped keys, storage namespaces, persistence commits, or active sessions; write secure storage, metadata storage, encrypted vault storage, Settings, files, or databases; add UI; enable vault creation, vault unlock, vault persistence, production provider use, provider selectability, or mainnet. OS keyrings remain rejected as primary storage and for Skald-managed passphrase storage, password managers remain rejected for Skald-managed passphrase storage, Settings/preferences storage remains rejected for secrets, creation state, and unlock state, provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, and passphrase-first remains the default authority.
- A still-disabled vault authorization/readiness matrix exists as model-only evidence for traceability across provider selection, provider operations, runtime randomness, KDF calibration, secure storage, vault creation, vault unlock, active sessions, persistence, storage, lifecycle, platform, wallet, and mainnet gates. It consolidates which boundary blocks each future capability and records required future evidence, prohibited accidental readiness flags, override rules, and redacted diagnostic classes. It does not enable any capability: every production/runtime capability remains blocked, warning-only evidence cannot authorize production use, user consent cannot override missing hard gates, test-only evidence cannot authorize production runtime, mainnet remains disabled, provider selection still selects only `DisabledVaultCryptoProvider`, and `productionProviderSelectable` remains false.
- A still-disabled provider candidate packaging boundary exists as model-only evidence for future crypto provider packaging, naming, dependency category, source-set placement, review gates, and promotion blockers. It models future Tink JVM, Bouncy Castle JVM, Android Keystore wrapper, platform OS CSPRNG, test-only deterministic, unknown, and unsupported candidates without implementing, instantiating, or selecting any provider. It does not add or activate dependencies, run provider operations or KATs, run randomness checks, run Argon2id/KDF/HKDF/HMAC/AEAD, generate keys, wrap keys, create or unlock vaults, persist storage, enable wallet behavior, or enable mainnet. Provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, and future promotion requires explicit later review branches.
- A still-disabled provider dependency/build boundary exists as model-only evidence for the provider dependency build spike. It records that the existing platform-scoped Tink/Bouncy declarations are build-only evidence from prior dependency work, so this branch adds no new Gradle dependency. It models Tink JVM, Bouncy Castle JVM, Android platform wrapper, OS CSPRNG/platform, test-only deterministic, unknown, and unsupported candidate families; declaration statuses; source-set placement; required gates; blockers; and disabled capabilities. It does not activate dependencies, import provider APIs in common production code, instantiate provider runtime, run provider operations or KATs, run randomness checks, run Argon2id/KDF/HKDF/HMAC/AEAD, add provider implementation/factory code, change provider selection, enable vault creation/unlock/persistence, or enable mainnet.
- A still-disabled provider selection promotion blocker boundary exists as model-only evidence for future provider promotion stages from candidate description through dependency/build evidence, implementation, factory, registry, test selectability, production selectability, operation authorization, creation/unlock/persistence integration, release validation, and mainnet approval. It keeps every stage beyond candidate description/build evidence blocked and records blockers from provider selection, `productionProviderSelectable=false`, provider operation authorization, provider candidate packaging, provider dependency build evidence, the authorization/readiness matrix, provider acceptance, dependency probe, KAT approval, runtime randomness, KDF calibration, secure-storage, creation, unlock, redaction/leakage, clear/wipe, migration/corruption, release review, and mainnet policy. It does not add dependencies, implement a provider, add a factory, enable a registry, instantiate provider code, run provider operations or KATs, run randomness checks, run KDF/HKDF/HMAC/AEAD, change provider selection, enable vault creation/unlock/persistence, or enable mainnet.
- A still-disabled provider interface contract audit exists as model-only evidence for provider-facing contract safety. It audits provider-neutral common contracts, the disabled provider facade, provider selection registry, provider operation authorization, provider candidate packaging, dependency/build evidence, promotion blockers, provider acceptance, KAT/randomness/KDF/secure-storage/creation/unlock gates, the authorization/readiness matrix, redaction/leakage, clear/wipe, and migration/corruption boundaries. It records that model/evidence paths carry enums/statuses/redacted tokens only and cannot accept secrets, byte material, provider handles, crypto objects, platform crypto imports, executable operations, provider selectability, `productionProviderSelectable=true`, vault creation, vault unlock, persistence, or mainnet.
- A still-disabled non-selectable provider skeleton boundary exists as model-only compile-shape evidence for future provider identity, source-set placement, dependency visibility, registry/selectability status, disabled operation surfaces, diagnostics/redaction, acceptance gates, promotion blockers, and readiness-matrix interaction. It does not implement executable provider behavior, implement `VaultCryptoProvider`, add a factory, enable a registry, instantiate provider code, run provider operations or KATs, use randomness, run KDF/HKDF/HMAC/AEAD, wrap keys, change provider selection, enable vault creation/unlock/persistence, or enable mainnet.
- A still-disabled provider registry isolation guard exists as model-only evidence for the current provider-selection registry. It records that the live registry remains isolated to `DisabledVaultCryptoProvider` and that non-selectable skeletons, candidate packaging evidence, dependency/build evidence, interface-audit evidence, promotion-blocker evidence, authorization/readiness matrix evidence, warning-only evidence, user consent, test-only evidence, and release/mainnet evidence cannot become non-disabled registry entries. It does not add dependencies, implement a provider, add a factory, enable a non-disabled registry, instantiate provider code, run provider operations or KATs, run randomness checks, run KDF/HKDF/HMAC/AEAD, change provider selection, enable vault creation/unlock/persistence, or enable mainnet.
- A still-disabled provider factory isolation boundary exists as model-only evidence for the absent non-disabled provider factory surface. It records that no non-disabled provider factory or constructor exists or is reachable from provider selection, registry isolation, skeleton evidence, candidate packaging evidence, dependency/build evidence, interface-audit evidence, promotion blockers, authorization/readiness matrix evidence, warning-only evidence, user consent, test-only evidence, or release/mainnet evidence. It does not add dependencies, implement a provider, add a provider factory, enable a non-disabled registry, instantiate provider code, expose provider handles or crypto objects, accept byte material, run provider operations or KATs, run randomness checks, run KDF/HKDF/HMAC/AEAD, change provider selection, enable vault creation/unlock/persistence, or enable mainnet.
- A still-disabled provider executable KAT decision gate exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_EXECUTABLE_KAT_DECISION_GATE.md`](docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_EXECUTABLE_KAT_DECISION_GATE.md). It defines future prerequisites before any executable provider-level KAT path may be introduced. It does not add a KAT executor, provider implementation, provider factory, provider selection, provider operation execution, vault creation/unlock/persistence, storage, production sync, or mainnet.
- A still-disabled provider executable KAT prerequisite audit exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_EXECUTABLE_KAT_PREREQUISITE_AUDIT.md`](docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_EXECUTABLE_KAT_PREREQUISITE_AUDIT.md). It maps decision-gate prerequisites to current model, documentation, and test-only evidence. It does not authorize executable KAT introduction and does not add a provider implementation, KAT executor, provider factory, provider dispatch path, provider operation execution, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only executable provider KAT scope decision exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_EXECUTABLE_PROVIDER_KAT_SCOPE_DECISION.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_EXECUTABLE_PROVIDER_KAT_SCOPE_DECISION.md). It defines future source-set and material limits for any later test-only executable KAT branch. It does not implement a KAT executor, authorize KAT execution now, authorize production provider selection, or add a provider implementation, factory, dispatcher, registry entry, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only provider KAT executor contract exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_EXECUTOR_CONTRACT.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_EXECUTOR_CONTRACT.md). It defines the non-executable contract for a future test-only executor, but it does not implement an executor, expose a runnable executor interface, authorize KAT execution now, authorize production provider selection, or add a provider implementation, factory, dispatcher, registry entry, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only provider KAT vector catalog exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_VECTOR_CATALOG.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_VECTOR_CATALOG.md). It defines future positive, negative, redaction, platform, provenance, and fixture policy classes as references only. It does not include raw vector material in common production source, implement an executor, authorize KAT execution now, authorize production provider selection, or add a provider implementation, factory, dispatcher, registry entry, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only provider KAT executor readiness gate exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_EXECUTOR_READINESS_GATE.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_EXECUTOR_READINESS_GATE.md). It evaluates whether the prior decision gate, prerequisite audit, scope decision, executor contract, and vector catalog can authorize executor implementation. It keeps executor implementation unauthorized now, does not implement an executor, does not expose a runnable executor interface, does not authorize KAT execution now, does not authorize production provider selection, and does not add a provider implementation, factory, dispatcher, registry entry, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only provider KAT source-set confinement boundary exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_SOURCE_SET_CONFINEMENT.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_KAT_SOURCE_SET_CONFINEMENT.md). It defines production-forbidden source sets, model-only source sets, evidence-only docs, and future-review-required test source sets. It keeps executor implementation unauthorized now, does not implement an executor, does not expose a runnable executor interface, does not authorize KAT execution now, does not authorize production provider selection, and does not add a provider implementation, factory, dispatcher, registry entry, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only provider identity decision exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_IDENTITY_DECISION.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_IDENTITY_DECISION.md). It models future test-only provider identity categories without implementing a provider. It does not expose an instantiable provider identity, registry-selectable provider identity, or executor-targetable provider identity, and it does not add a provider implementation, factory, dispatcher, registry entry, executor target, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- A still-disabled test-only provider identity isolation guard exists as model-only evidence and is documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_IDENTITY_ISOLATION_GUARD.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_ONLY_PROVIDER_IDENTITY_ISOLATION_GUARD.md). It keeps modeled test-only identity categories label-only and absent from registry, factory, dispatcher, executor targets, vault lifecycle, persistence, secure storage, secure metadata, production sync, wallet services, BDK paths, settings codecs, and UI. It does not implement a provider, expose an instantiable identity, expose a registry-selectable identity, expose an executor-targetable identity, or add a provider implementation, factory, dispatcher, registry entry, executor target, provider operations, randomness, KDF/HKDF/HMAC/AEAD, vault lifecycle, persistence, production sync, or mainnet. Provider selection remains disabled-provider-only and `productionProviderSelectable` remains false.
- The app-controlled encrypted local vault architecture and key-lifecycle plan is documented in [`docs/ENCRYPTED_LOCAL_VAULT_DESIGN.md`](docs/ENCRYPTED_LOCAL_VAULT_DESIGN.md), with crypto/key-lifecycle decisions recorded in [`docs/ENCRYPTED_LOCAL_VAULT_CRYPTO_DECISION.md`](docs/ENCRYPTED_LOCAL_VAULT_CRYPTO_DECISION.md). Code-level readiness models are documented in [`docs/ENCRYPTED_VAULT_READINESS_POLICY.md`](docs/ENCRYPTED_VAULT_READINESS_POLICY.md). The focused dependency spike is documented in [`docs/ENCRYPTED_LOCAL_VAULT_DEPENDENCY_SPIKE.md`](docs/ENCRYPTED_LOCAL_VAULT_DEPENDENCY_SPIKE.md), with desktop and Android runtime public-vector validation documented in [`docs/ENCRYPTED_LOCAL_VAULT_KAT_VALIDATION.md`](docs/ENCRYPTED_LOCAL_VAULT_KAT_VALIDATION.md), libsodium/Kotlin comparison documented in [`docs/ENCRYPTED_LOCAL_VAULT_LIBSODIUM_COMPARISON.md`](docs/ENCRYPTED_LOCAL_VAULT_LIBSODIUM_COMPARISON.md), Tink/Bouncy dependency review documented in [`docs/ENCRYPTED_LOCAL_VAULT_DEPENDENCY_REVIEW.md`](docs/ENCRYPTED_LOCAL_VAULT_DEPENDENCY_REVIEW.md), the disabled provider boundary documented in [`docs/ENCRYPTED_LOCAL_VAULT_CRYPTO_PROVIDER_BOUNDARY.md`](docs/ENCRYPTED_LOCAL_VAULT_CRYPTO_PROVIDER_BOUNDARY.md), the disabled provider-selection boundary documented in [`docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_SELECTION_BOUNDARY.md`](docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_SELECTION_BOUNDARY.md), the provider-level KAT contract documented in [`docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_KAT_CONTRACT.md`](docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_KAT_CONTRACT.md), the vault container/manifest/storage contract documented in [`docs/ENCRYPTED_LOCAL_VAULT_CONTAINER_MANIFEST_STORAGE_CONTRACT.md`](docs/ENCRYPTED_LOCAL_VAULT_CONTAINER_MANIFEST_STORAGE_CONTRACT.md), the test-only provider KAT harness documented in [`docs/ENCRYPTED_LOCAL_VAULT_TEST_PROVIDER_KAT_HARNESS.md`](docs/ENCRYPTED_LOCAL_VAULT_TEST_PROVIDER_KAT_HARNESS.md), Argon2id calibration policy/probe planning documented in [`docs/ENCRYPTED_LOCAL_VAULT_ARGON2ID_CALIBRATION.md`](docs/ENCRYPTED_LOCAL_VAULT_ARGON2ID_CALIBRATION.md), non-final Argon2id parameter policy documented in [`docs/ENCRYPTED_LOCAL_VAULT_ARGON2ID_PARAMETER_POLICY.md`](docs/ENCRYPTED_LOCAL_VAULT_ARGON2ID_PARAMETER_POLICY.md), manual Android calibration evidence capture documented in [`docs/ENCRYPTED_LOCAL_VAULT_ANDROID_CALIBRATION_CAPTURE.md`](docs/ENCRYPTED_LOCAL_VAULT_ANDROID_CALIBRATION_CAPTURE.md), Android compatibility/entropy policy documented in [`docs/ENCRYPTED_LOCAL_VAULT_ANDROID_COMPATIBILITY_ENTROPY_POLICY.md`](docs/ENCRYPTED_LOCAL_VAULT_ANDROID_COMPATIBILITY_ENTROPY_POLICY.md), runtime randomness/provider checks documented in [`docs/ENCRYPTED_LOCAL_VAULT_RUNTIME_RANDOMNESS_PROVIDER_CHECKS.md`](docs/ENCRYPTED_LOCAL_VAULT_RUNTIME_RANDOMNESS_PROVIDER_CHECKS.md), the Tink raw-key feasibility probes documented in [`docs/ENCRYPTED_LOCAL_VAULT_TINK_RAW_KEY_FEASIBILITY_PROBE.md`](docs/ENCRYPTED_LOCAL_VAULT_TINK_RAW_KEY_FEASIBILITY_PROBE.md), the header commitment/AAD construction contract documented in [`docs/ENCRYPTED_LOCAL_VAULT_HEADER_COMMITMENT_AAD_CONTRACT.md`](docs/ENCRYPTED_LOCAL_VAULT_HEADER_COMMITMENT_AAD_CONTRACT.md), the HKDF-SHA-256 key-expansion and HMAC-SHA-256 header-commitment policy documented in [`docs/ENCRYPTED_LOCAL_VAULT_KEY_EXPANSION_COMMITMENT_POLICY.md`](docs/ENCRYPTED_LOCAL_VAULT_KEY_EXPANSION_COMMITMENT_POLICY.md), the canonical header/HKDF/HMAC non-secret vector contract documented in [`docs/ENCRYPTED_LOCAL_VAULT_CANONICAL_HEADER_HKDF_HMAC_VECTORS.md`](docs/ENCRYPTED_LOCAL_VAULT_CANONICAL_HEADER_HKDF_HMAC_VECTORS.md), and the v1 production-provider acceptance contract documented in [`docs/ENCRYPTED_LOCAL_VAULT_PRODUCTION_PROVIDER_ACCEPTANCE_CONTRACT.md`](docs/ENCRYPTED_LOCAL_VAULT_PRODUCTION_PROVIDER_ACCEPTANCE_CONTRACT.md): pinned Tink and Bouncy Castle platform probes expose candidate XChaCha20-Poly1305 and Argon2id APIs, pass desktop JVM KATs plus Android instrumented runtime KATs on Pixel 10 Pro XL / Android 16, and now have candidate-level dependency/license/keyset/split-provider review evidence plus desktop/JVM and Android raw-key feasibility results of `FEASIBLE_PUBLIC_RAW_KEY_API` and `ANDROID_FEASIBLE_PUBLIC_RAW_KEY_API`. The provider boundary is Skald-owned and disabled; provider-wired Argon2id passphrase derivation, provider-wired Tink AEAD execution, key generation, keyset storage, provider-level KAT, and provider operations fail closed. The passphrase policy validator/NFC UTF-8 encoder, explicit-parameter Bouncy Castle Argon2id passphrase-to-root derivation, canonical header serializer, HKDF-SHA-256 expansion from caller-supplied 64-byte root material, HMAC-SHA-256 header commitment computation/verification, strict AAD serialization, Tink record AEAD construction from caller-supplied 32-byte key material, and in-memory container parser/writer now exist as still-disabled building blocks that match fixed non-secret tests where applicable; they do not implement vault unlock, provider-selectable record decrypt, vault creation, or persistence. The container parser/writer operates only on caller-supplied byte arrays, validates non-secret container syntax/header evidence/record placeholders/manifest placeholders with typed failures, and performs no file I/O, manifest storage, filesystem storage, database storage, secure-storage calls, KDF/HKDF/HMAC/Tink execution, or full rollback-resistance enforcement. A metadata-only still-disabled provider facade now reports the suite id, selected policy ids, existing building-block evidence, disabled reasons, remaining gates, and typed disabled operation results; it is not referenced by the selection registry and does not expose usable create, unlock, storage, manifest, secure-storage, wallet, sync, or backend APIs. The provider-selection boundary selects only the disabled provider; Tink plus Bouncy Castle is listed as a blocked future candidate, Lazysodium Java/Android remains rejected for this vault branch, and IonSpin KMP libsodium remains deferred. The v1 acceptance contract records the rationale and gates for Bouncy Castle Argon2id, HKDF-SHA-256 key expansion, HMAC-SHA-256 header commitment, Tink XChaCha20-Poly1305, OS SecureRandom, passphrase-first protection, optional Android wrapping, non-key-committing AEAD handling, canonical header commitment, key-separation labels, strict AAD binding, and a single pinned provider suite, but it cannot make a provider selectable. The provider-level KAT contract models future positive, negative, platform, nonce-policy, redaction, and storage-separation requirements, and a test-only provider harness proves those public vectors and required negative cases can route through the Skald-owned interface on desktop and Android runtime. The Argon2id policy keeps calibration non-final; current Pixel evidence remains high-end debug/instrumented timing evidence only and does not prove all-device performance. Low-end and mid-range Android model testing are no longer hard blockers for compatibility planning; supported Android baseline plus runtime provider/primitive/randomness checks and fail-closed vault creation gates now define the Android compatibility policy. OS or reviewed-provider cryptographic randomness is required for vault material; language/random utility APIs are forbidden, test-only Android/Linux randomness probes are availability checks only, and hardware-backed key protection remains optional future wrapping, not the randomness source. None of these docs, probes, harnesses, evidence models, provider models, facade metadata, disabled results, or still-disabled building blocks implement provider-selectable encryption, entropy collection, vault persistence, production secret persistence, production metadata persistence, production sync, integrated Tor, external Tor connections, or mainnet. OS keyrings are not primary storage; they may later wrap vault keys only after explicit design review.
- Production observation persistence is explicitly deferred until app-controlled encrypted local vault storage exists and is approved. Observed addresses, UTXOs, labels, transaction notes, backend metadata, address index state, and wallet history are sensitive wallet metadata and are not persisted by this scaffold.
- A desktop-test-only local Electrum-compatible regtest indexer harness boundary exists and is documented in [`docs/LOCAL_ELECTRUM_REGTEST_HARNESS.md`](docs/LOCAL_ELECTRUM_REGTEST_HARNESS.md). It is opt-in test infrastructure only. With a local `electrs` binary configured through `SKALD_ELECTRS`, the harness starts against temporary local regtest `bitcoind` state and cleans up afterward.
- A desktop-test-only BDK regtest UTXO scan validation boundary and BDK Electrum scan adapter exist and are documented in [`docs/BDK_REGTEST_UTXO_SCAN_VALIDATION.md`](docs/BDK_REGTEST_UTXO_SCAN_VALIDATION.md). The current BDK JVM artifact exposes wallet scan requests and indexed backends, but no direct local-node RPC scan backend. The adapter uses only a local `electrs` regtest endpoint when both opt-in flags are set; local validation now observes a funded runtime regtest UTXO and applies receive-address used-state policy. No production wallet sync or app UTXO scan is enabled.
- A local `bitcoind` regtest harness exists for desktop development tests only. It is documented in [`docs/REGTEST_HARNESS.md`](docs/REGTEST_HARNESS.md). It uses temporary regtest state and does not connect the app UI to a backend, create a Skald or BDK wallet, persist wallet databases, sync app wallets, sign, broadcast, or enable mainnet.
- Coin-control and PSBT draft planning uses demo UTXO placeholders only. No real UTXOs are scanned, no transaction is constructed, no PSBT is created, and signing/broadcasting remain disabled.
- Backend profiles, UTXO views, coin-control drafts, and PSBT workflows are non-operational placeholder models only.
- Bitcoin backend profiles are local non-secret configuration state only. The app does not store backend credentials, run real backend connection tests, sync wallets, scan chains, derive addresses, sign, or broadcast.
- The backend connection-test harness is simulated validation only. It checks profile metadata and planned future test steps, but it does not perform DNS, socket, RPC, Electrum, Esplora, HTTP, Tor/proxy, chain-sync, fee-estimation, wallet-query, or broadcast network calls.
- A secure-storage abstraction exists for future secret-bearing features, but all secret storage is disabled/not implemented. Secret writes, reads, metadata listing, and deletes fail closed. The future secure-storage design is documented in [`docs/SECURE_STORAGE_DESIGN.md`](docs/SECURE_STORAGE_DESIGN.md).

## No Managed Infrastructure

Skald Vault is client software. It does not operate wallet infrastructure. Users must configure their own nodes, mints, LSPs, relays, Payjoin directories, and backends when those features are implemented.

The scaffold includes no Skald-operated defaults for:

- Bitcoin Core, Electrum, or Esplora backends
- Lightning nodes or LSPs
- Cashu mints
- Nostr relays
- Payjoin coordinators or directories
- Analytics, notification relays, or fee-taking transaction services

## Architecture Notes

- `composeApp/src/commonMain` holds shared domain models and the shared Compose UI. The UI is split into app shell/navigation, reusable Skald components, and focused screen files.
- `composeApp/src/androidMain` holds the Android launcher.
- `composeApp/src/desktopMain` holds the Linux desktop launcher.
- Domain packages now separate core rails/network state, descriptor-native on-chain models, backend configuration models, strict coin-control models, disabled PSBT workflow models, recovery state, quote modeling, privacy warnings, and Lightning/Cashu/Nostr placeholders.
- `settings` holds small repositories/codecs for user-editable, persistent, non-secret Bitcoin backend profiles, descriptor wallet profile metadata, and coin-control/PSBT draft metadata. Android uses SharedPreferences and desktop uses local config files; these stores only hold non-secret labels, enum/status values, development network choices, backend endpoint fields, trust classification, demo UTXO IDs, draft notes/amounts, acknowledgements, blockers, and disabled workflow states.
- `security` holds the secure-storage service boundary, secure metadata persistence boundary, disabled vault crypto provider boundary, disabled provider-selection registry, and disabled implementations. Android and Linux desktop currently report secure storage as unavailable and do not store seeds, private keys, Nostr nsecs, node credentials, Cashu material, backup keys, observed addresses, UTXO state, address indexes, labels, transaction notes, or wallet history.
- `docs/SECURE_STORAGE_DESIGN.md` defines the future Android/Linux secure-storage design gates. It is a design document only; it does not enable real secret persistence.
- `docs/ENCRYPTED_LOCAL_VAULT_DESIGN.md` defines the app-controlled encrypted local vault architecture, key hierarchy, platform strategy, Tor/network metadata implications, recovery implications, migration/corruption handling, and acceptance criteria required before real secret or sensitive metadata persistence. It is a design document only; it does not implement encryption or persistence.
- `docs/ENCRYPTED_LOCAL_VAULT_CRYPTO_DECISION.md` records the vault crypto/key-lifecycle design decisions: Argon2id as the target KDF, XChaCha20-Poly1305 as the target record AEAD for the candidate-reviewed stack, per-record random nonce policy, key hierarchy, platform wrapping policy, container/versioning strategy, backup/export strategy, corruption/migration behavior, and readiness gates. It is a design record only; it does not add runtime cryptography.
- `docs/ENCRYPTED_LOCAL_VAULT_DEPENDENCY_SPIKE.md` records the vault crypto dependency probe. Tink `1.21.0` and Bouncy Castle `1.84` are pinned in platform source sets so Android and Linux packaging can verify candidate API presence; no vault storage, KDF implementation, AEAD implementation, key generation, persistence, or unlock UI is enabled.
- `SkaldVaultV1ProviderDependencyBuildPolicy` records the current provider dependency/build spike as still-disabled evidence. No new Gradle dependency was added because the existing platform-scoped Tink/Bouncy declarations already provide declared/resolvable build evidence and the current design remains a split stack. The model does not activate dependencies, instantiate providers, run crypto or KATs, change provider selection, or enable vault creation/unlock/persistence.
- `SkaldVaultV1ProviderInterfaceContractAuditPolicy` records the current provider-facing interface audit as still-disabled evidence. It does not add dependencies, provider implementation/factory/registry code, provider handles, crypto objects, byte material, executable operations, provider selection, provider promotion, vault creation/unlock/persistence, or mainnet.
- `SkaldVaultV1NonSelectableProviderSkeletonPolicy` records the current non-selectable provider skeleton as still-disabled compile-shape evidence. It models the future provider skeleton name/family/source-set/dependency visibility and every operation surface as disabled, but it does not implement a provider, register a provider, make a provider selectable, execute crypto, run KATs, or enable vault creation/unlock/persistence.
- `SkaldVaultV1ProviderRegistryIsolationGuardPolicy` records the current provider-selection registry isolation as still-disabled evidence. It proves the registry can represent only the disabled provider path today, excludes skeleton/candidate/evidence models from registry entries, and does not add a provider factory, non-disabled registry entry, runtime instantiation path, provider operation path, provider KAT path, crypto, storage, vault creation/unlock/persistence, or mainnet.
- `SkaldVaultV1ProviderFactoryIsolationPolicy` records the current provider-factory isolation boundary as still-disabled evidence. It proves no non-disabled provider factory or constructor exists, no factory path is reachable from selection, registry isolation, skeleton, candidate packaging, dependency/build, promotion, or matrix evidence, and no factory can expose provider handles, crypto objects, byte material, provider operations, KAT execution, randomness, KDF/HKDF/HMAC/AEAD, key wrapping, vault creation/unlock/persistence, or mainnet.
- `SkaldVaultV1ProviderOperationDispatchIsolationPolicy` records the current provider-operation dispatch isolation boundary as still-disabled evidence. It proves no non-disabled provider operation dispatcher exists or is reachable from selection, registry isolation, factory isolation, skeleton, candidate packaging, dependency/build, promotion, matrix, creation/unlock/storage readiness, or persistence evidence, and no dispatcher can invoke provider runtime code, expose provider handles or crypto objects, accept byte material, run provider operations or KATs, call randomness, run KDF/HKDF/HMAC/AEAD, compute header commitments, encrypt/decrypt records, wrap keys, enable vault creation/unlock/persistence, or enable mainnet.
- `SkaldVaultV1ProviderKatExecutionIsolationPolicy` records the current provider KAT execution isolation boundary as still-disabled evidence. It proves no non-disabled provider KAT executor exists or is reachable from selection, registry isolation, factory isolation, dispatch isolation, skeleton, candidate packaging, dependency/build, promotion, matrix, creation/unlock/storage readiness, persistence evidence, public vector docs, or test-provider harness concepts, and no KAT/vector/self-test path can execute provider crypto, authorize provider selection, set `productionProviderSelectable=true`, enable vault creation/unlock/persistence, or enable mainnet.
- `SkaldVaultV1ProviderExecutableKatDecisionGatePolicy` records the current decision boundary for future executable provider KAT work as still-disabled evidence. It enumerates future evidence required before a later branch may introduce a test-only executable provider-level KAT path, while keeping executable KATs, provider operations, provider selection authorization, `productionProviderSelectable=true`, vault creation/unlock/persistence, storage, production sync, and mainnet blocked.
- `SkaldVaultV1TestOnlyProviderKatExecutorContractPolicy` records the non-executable contract for any future test-only provider KAT executor. It models allowed future label/reference inputs, forbidden material classes, future test-only operation categories, source-set constraints, result redaction rules, and authorization limits while keeping executor implementation, executor callability, provider selection, `productionProviderSelectable=true`, vault creation/unlock/persistence, production sync, and mainnet blocked.
- `SkaldVaultV1TestOnlyProviderKatVectorCatalogPolicy` records the reference-only catalog for future test-only provider KAT vector classes. It models future positive, negative, redaction, platform, provenance, and fixture policy classes without raw vector material, executor implementation, provider operation execution, provider selection, `productionProviderSelectable=true`, vault lifecycle, persistence, production sync, or mainnet.
- `SkaldVaultV1TestOnlyProviderKatExecutorReadinessGatePolicy` records the readiness gate for any later test-only provider KAT executor implementation branch. It composes the prior model-only decision, audit, scope, contract, and vector-catalog evidence, classifies requirements, and keeps executor implementation, runnable interfaces, provider operation execution, provider selection, `productionProviderSelectable=true`, vault lifecycle, persistence, production sync, and mainnet blocked.
- `SkaldVaultV1TestOnlyProviderKatSourceSetConfinementPolicy` records the source-set confinement boundary for any later test-only provider KAT executor branch. It models production-forbidden, model-only, evidence-only, and future-review-required source-set categories while keeping executor implementation, runnable interfaces, raw vector material, provider operation execution, provider selection, `productionProviderSelectable=true`, vault lifecycle, persistence, production sync, and mainnet blocked.
- `SkaldVaultV1TestOnlyProviderIdentityDecisionPolicy` records the identity decision boundary for any later test-only provider KAT executor branch. It models future test-only provider identity categories while keeping provider implementation, instantiation, registry selection, factory creation, dispatcher routing, executor targeting, provider operation execution, provider selection, `productionProviderSelectable=true`, vault lifecycle, persistence, production sync, and mainnet blocked.
- `SkaldVaultV1TestOnlyProviderIdentityIsolationGuardPolicy` records the identity isolation guard for the modeled test-only provider identity categories. It keeps those categories label-only and absent from registry, factory, dispatcher, executor target, vault lifecycle, persistence, secure-storage, secure-metadata, production-sync, wallet-service, BDK, settings, UI, and mainnet paths while keeping provider implementation, provider selection, `productionProviderSelectable=true`, vault lifecycle, persistence, production sync, and mainnet blocked.
- `docs/ENCRYPTED_LOCAL_VAULT_KAT_VALIDATION.md` records desktop JVM and Android runtime public known-answer-vector validation for Bouncy Castle Argon2id and Tink XChaCha20-Poly1305. It is dependency validation only; Android runtime KATs passed on Pixel 10 Pro XL / Android 16 after raw ADB IP:port targeting, and secure storage remains disabled.
- `docs/ENCRYPTED_LOCAL_VAULT_LIBSODIUM_COMPARISON.md` records the libsodium/Kotlin comparison. Lazysodium Java/Android `5.2.0` exposes relevant APIs but failed Android packaging with duplicate JNA classes and is not left on the runtime classpath; IonSpin KMP libsodium `0.9.5` remains deferred after metadata/POM inspection.
- `docs/ENCRYPTED_LOCAL_VAULT_DEPENDENCY_REVIEW.md` records the Tink/Bouncy dependency, license, Tink keyset/storage, Bouncy Castle Argon2id API, and split-provider review. It promotes the stack only to a candidate-reviewed status; it does not approve production implementation or storage.
- `docs/ENCRYPTED_VAULT_READINESS_POLICY.md` records the code-level encrypted vault readiness models. The models report the vault as not implemented, dependency selection reviewed only at candidate level, KDF/AEAD parameters unverified, candidate KDF parameter policy not final, storage disabled, production persistence blocked, and mainnet disabled. They do not implement encryption, storage, unlock UI, or runtime cryptography.
- `docs/ENCRYPTED_LOCAL_VAULT_CRYPTO_PROVIDER_BOUNDARY.md` records the disabled Skald-owned provider boundary. It models typed provider requests/results/errors, algorithm policy, nonce policy, record purposes, provider-level KAT requirements, and redacted diagnostics while rejecting KDF execution, AEAD execution, key generation, keyset storage, container read/write, persistence, and mainnet.
- `docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_SELECTION_BOUNDARY.md` records the disabled provider-selection registry. It selects `DisabledVaultCryptoProvider` only, lists Tink plus Bouncy Castle as a blocked future candidate, keeps Lazysodium rejected and IonSpin deferred, and treats dependency-level KATs, test-provider KATs, runtime Android compatibility/randomness checks, disabled storage, vault-container review, and mainnet as selection gates.
- `docs/ENCRYPTED_LOCAL_VAULT_PROVIDER_KAT_CONTRACT.md` records the provider-level KAT and stale-record manifest contracts that a future executable Skald-owned provider and storage layer must satisfy. It now also records the still-disabled integrated provider KAT harness that composes the existing building blocks with fixed non-secret fixtures, asserts deterministic vectors, validates randomized AEAD behavior without fixed ciphertext hex, and proves header commitment before record AEAD use. It does not make a provider selectable or implement manifest storage.
- `docs/ENCRYPTED_LOCAL_VAULT_CONTAINER_MANIFEST_STORAGE_CONTRACT.md` records the future v1 vault container, manifest, storage, stale-record, rollback, atomicity, crash-recovery, and secure-storage boundary contract. It now also records model-only Android/desktop platform storage-root, platform root settings, safe path-construction, symlink/traversal, permission/ownership, durability-capability, durability fail-closed, and warning-only rejection contracts. Android v1 vault storage is app-private internal storage only; Android external/shared and arbitrary user-selected roots are rejected. Linux defaults to a user-data root conventionally under `~/.local/share/`, with future custom Linux roots planned only through Settings after static validation and review. A still-disabled Linux custom-root validation policy now accepts only static policy-valid candidate strings and rejects relative paths, tilde paths, shared/system/temp roots, traversal, ambiguous characters, labels, and secret-looking material; accepted candidates are not resolved roots and do not prove existence, containment, permissions, symlink state, durability, or persistence readiness. OS keyrings and password-manager integration are rejected for Skald-managed vault passphrase storage, and passphrase-first remains the default authority. Unsupported, unknown, unreviewed, insufficient, unsafe, or failed durability blocks encrypted vault persistence; user consent cannot override that failure for v1 vault writes. Still-disabled in-memory container and manifest parser/writer building blocks validate caller-supplied byte arrays against non-secret fixtures and typed failure models, and the local stale-record decision policy classifies current, newer, stale, conflicting, and unknown record descriptors relative to an in-memory manifest. The still-disabled logical storage layout plan composes validated namespace/path policy segments into deterministic rootless relative segment lists for current container, current manifest, current storage index, records, temporary artifacts, quarantine, and recovery metadata. The still-disabled path-containment planner binds those relative segment lists to reviewed root tokens and typed artifact kinds without resolving roots or constructing platform paths. The still-disabled platform path-construction boundary consumes platform root resolver evidence plus logical layout evidence and returns typed redacted planned artifact-location evidence only; it does not construct real or absolute paths, return `File`/`Path`/`Uri` objects, create directories, read files, write files, persist settings, prove existence, prove containment, prove symlink safety, prove permissions, prove ownership, prove durability, or enable persistence/provider selection. The still-disabled storage safety preflight boundary consumes planned artifact-location evidence and future safety-gate evidence, but only produces redacted fail-closed preflight evidence; it does not run filesystem checks, prove containment/symlink/permission/ownership/durability/atomic/crash safety, enable manifest/storage-index/record read/write, or enable persistence/provider selection. The disabled vault storage service facade consumes planned artifact-location evidence and storage safety preflight evidence to model future initialize/read/write/list/delete/quarantine/recovery/health/failure/atomic/close operations, but every operation returns disabled or rejected evidence and no operation returns success. The still-disabled vault persistence readiness gate composes provider selection, provider acceptance, dependency, root, planned artifact-location, storage safety preflight, disabled storage service facade, secure secret storage, and secure metadata evidence into one blocked/fail-closed decision. It does not run filesystem checks, construct real or absolute paths, create directories, read files, write files, persist settings, read/write manifests, read/write storage indexes, read/write/list/delete/quarantine/recover records, perform atomic writes, perform crash recovery, map real storage failures, approve provider use, approve mainnet, enable vault persistence, or enable provider selection. These building blocks and model-only contracts do not add file I/O, Settings UI, settings persistence, actual path construction, absolute path construction, platform root selection/resolution, real path containment checks, symlink checks, permission checks, durability probes, OS keyring integration, password-manager integration, warning-only persistence, vault persistence, manifest file/storage read/write, storage index read/write, record read/write/list/delete/quarantine/recovery, storage success, secure-storage success, global freshness, or a full rollback-resistance claim without an anti-rollback anchor.
- `skald-vault-v1-linux-root-resolution-policy-v1` is now represented as a still-disabled common model. It consumes caller-supplied static default-root evidence or accepted Linux custom-root validation evidence and returns redacted root-resolution evidence only. It does not read `HOME`, `XDG_DATA_HOME`, environment variables, or system properties; does not resolve or construct platform paths; does not call filesystem APIs; does not prove existence, containment, symlink safety, permissions, ownership, durability, or atomic-write safety; does not add Settings UI or settings persistence; does not make custom roots usable; and does not enable vault persistence or provider selectability.
- `docs/ENCRYPTED_LOCAL_VAULT_TEST_PROVIDER_KAT_HARNESS.md` records the test-only provider-level KAT harness. It proves the Skald-owned provider request/result path can carry public Argon2id and XChaCha vectors plus required negative cases on desktop and Android runtime, while production provider execution, vault containers, storage, persistence, and mainnet remain absent.
- `docs/ENCRYPTED_LOCAL_VAULT_ARGON2ID_CALIBRATION.md` records the still-disabled Argon2id calibration policy building block and probe-only measurement plan. It models explicit KiB/MiB memory, the shared 64 MiB / t=3 / p=1 / 64-byte v1 floor, deterministic candidate selection, about-1-second preferred and about-2-second acceptable timing, fail-closed floor execution failure, stored-parameter authority, no silent downgrade, and bounded desktop/Android test harnesses while keeping final production calibration approval, provider-wired KDF execution, storage, persistence, and mainnet disabled.
- `docs/ENCRYPTED_LOCAL_VAULT_ARGON2ID_PARAMETER_POLICY.md` records the non-final candidate Argon2id parameter policy: 64 MiB / 3 passes / 1 lane / 64-byte output as the shared v1 floor, 96 MiB / 3 passes / 1 lane / 64-byte output as a stronger desktop candidate, historical 16/32 MiB timing evidence as below-floor evidence only, and supported-Android compatibility planning based on runtime provider/randomness gates rather than mandatory low-end or mid-range model testing. It does not approve production parameters.
- `docs/ENCRYPTED_LOCAL_VAULT_ANDROID_CALIBRATION_CAPTURE.md` records the manual Android calibration evidence-capture protocol and template. It keeps Pixel 10 Pro XL / Android 16 evidence high-end only, rejects ambiguous units and secret-like evidence fields, and cannot approve calibration or provider-selectable KDF execution; low-end and mid-range model evidence is optional parameter/UX evidence, not a compatibility hard blocker.
- `docs/ENCRYPTED_LOCAL_VAULT_ANDROID_COMPATIBILITY_ENTROPY_POLICY.md` records Android supported-baseline compatibility and entropy policy. It separates OS/reviewed-provider cryptographic randomness from optional hardware-backed key protection, forbids language and ad hoc random APIs for vault material, and requires fail-closed user-facing vault-creation warnings when platform/provider/randomness/storage gates are missing or unknown.
- `docs/ENCRYPTED_LOCAL_VAULT_RUNTIME_RANDOMNESS_PROVIDER_CHECKS.md` records runtime randomness/provider check models and test-only Android/Linux availability probes. The probes generate only small non-secret samples, do not persist or log samples, do not prove entropy quality, and do not enable production randomness, key generation, provider crypto, vault storage, or mainnet.
- `docs/ENCRYPTED_LOCAL_VAULT_TINK_RAW_KEY_FEASIBILITY_PROBE.md` records the test-scope Tink raw-key feasibility results `FEASIBLE_PUBLIC_RAW_KEY_API` on desktop/JVM and `ANDROID_FEASIBLE_PUBLIC_RAW_KEY_API` on Android. Public Tink APIs can construct the pinned XChaCha20-Poly1305 primitive from caller-supplied fixed raw key bytes through a transient in-memory keyset handle, with no persisted keyset, no random Tink-generated vault key, no key rotation, no multiple active keys, no internal APIs, and no reflection. The still-disabled record AEAD building block uses that public API family, but neither the probes nor the building block approve provider selection.
- `docs/ENCRYPTED_LOCAL_VAULT_KEY_EXPANSION_COMMITMENT_POLICY.md` records the selected v1 HKDF-SHA-256 key-expansion policy, HMAC-SHA-256 header-commitment primitive, 64-byte Argon2id root material, 32-byte header commitment key, 32-byte record AEAD key, verification order, and threat-model limits. The matching canonical header serializer, HKDF expansion, and HMAC verification now exist as still-disabled building blocks; they do not implement Argon2id, passphrase handling, AEAD, vault storage, or provider selection.
- `docs/ENCRYPTED_LOCAL_VAULT_CANONICAL_HEADER_HKDF_HMAC_VECTORS.md` records deterministic non-secret canonical header bytes, HKDF-SHA-256 input/info/output vectors, and the HMAC-SHA-256 header-commitment vector. The still-disabled building blocks match those vectors and do not implement vault unlock, record decrypt, vault creation, or persistence.
- `docs/ENCRYPTED_LOCAL_VAULT_PRODUCTION_PROVIDER_ACCEPTANCE_CONTRACT.md` records the v1 production-provider acceptance contract and rationale. `docs/ENCRYPTED_LOCAL_VAULT_HEADER_COMMITMENT_AAD_CONTRACT.md` records the focused header commitment, canonical header encoding, key-separation label, strict AAD construction contract, deterministic AAD fixture bytes, and still-disabled record AEAD behavior tests. Together they pin the review direction to Bouncy Castle Argon2id, Tink XChaCha20-Poly1305, and OS SecureRandom with passphrase-first protection, optional Android wrapping, one explicit suite id, non-key-committing AEAD handling, a separate canonical header commitment before record decrypt, the `unicode-nfc-utf8-no-controls-no-whitespace-v1` passphrase encoding policy, passphrase-derived raw AEAD key material with public Tink API feasibility evidence, bounded Argon2id calibration, and fail-closed acceptance gates. They do not implement or select a production provider.
- `docs/DESCRIPTOR_KEY_MANAGEMENT_DESIGN.md` defines the future descriptor parser, wallet, key-management, PSBT, recovery, and protocol-library integration gates. It is a design document only; it does not enable descriptor parsing, key management, address derivation, or wallet operations.
- `docs/BDK_INTEGRATION_SPIKE.md` records the first BDK packaging spike. BDK is present only as platform dependencies behind a Skald-owned adapter probe; common UI/domain/settings models still do not expose BDK types.
- `docs/REGTEST_HARNESS.md` records the local `bitcoind` regtest harness. The harness is opt-in desktop test infrastructure and does not add production backend networking or app wallet behavior.
- `docs/LOCAL_ELECTRUM_REGTEST_HARNESS.md` records the local Electrum-compatible regtest indexer harness boundary. The harness is opt-in desktop test infrastructure and does not add production Electrum support, public backend defaults, or app wallet sync.
- `docs/BDK_JVM_LINUX_BINDING_DECISION.md` records the BDK JVM/Linux native binding decision and the accepted `2.3.0` pin.
- `docs/BDK_REGTEST_WALLET_VALIDATION.md` records the desktop-test-only seed-backed BDK regtest wallet validation boundary.
- `docs/BDK_REGTEST_ADDRESS_DERIVATION.md` records the desktop-test-only BDK regtest/signet address derivation validation boundary. The app still has no production receive-address flow or address index persistence.
- `docs/RECEIVE_ADDRESS_POLICY.md` records the production-safe receive-address state model and reuse-prevention policy. It is a policy/model document only; it does not enable production address derivation, address persistence, backend sync, or receive UI.
- `docs/BACKEND_OBSERVATION_STATE.md` records the Skald-owned backend observation and UTXO state boundary. It is a policy/model document only; it does not enable production backend sync, UTXO persistence, app UTXO display, signing, broadcasting, public endpoints, or mainnet.
- `docs/PRODUCTION_BACKEND_ADAPTER_BOUNDARY.md` records the disabled production backend adapter interface and endpoint normalization boundary. It is a policy/model document only; it does not enable production Bitcoin Core RPC, Electrum, Esplora, BDK sync, UTXO persistence, signing, broadcasting, public endpoints, or mainnet.
- `docs/PRODUCTION_SYNC_SERVICE_BOUNDARY.md` records the disabled production sync service facade and the Nodes preflight/status surface. It is a policy/model/status document only; it does not enable production wallet sync, backend clients, observation persistence, receive UI, signing, broadcasting, public endpoints, or mainnet.
- `docs/RECOVERY_PRIVACY_SYNC_STATUS.md` records the Recovery Center and Privacy Analyzer status integration for disabled sync, backend observation, receive-address policy, secure-storage blockers, and encrypted-vault persistence deferral. It is a status document only; it does not enable production observation persistence or wallet recovery state.
- `docs/SECURE_METADATA_BOUNDARY.md` records the disabled/fail-closed secure metadata persistence boundary. It is a policy/interface document only; it does not implement encrypted vault storage, production observation persistence, production address index persistence, or wallet metadata storage.
- `docs/BDK_REGTEST_UTXO_SCAN_VALIDATION.md` records the desktop-test-only BDK regtest UTXO scan validation boundary and local Electrum scan adapter. It does not enable production sync or app UTXO scanning.
- `docs/PHASE1_COMPLETION_AUDIT.md` records the Phase 1 hardening/completion audit. It is an audit artifact only; it does not enable wallet functionality.
- `DemoPortfolioRepository` is static design-preview data only. It is deliberately named as demo state and must not be treated as live wallet data.
- `DemoOnChainRepository`, `DemoCoinControlRepository`, `FakeBitcoinBackendConnectionTester`, and `DemoRecoveryRepository` drive Phase 1 UI scaffolding with sentinel placeholders such as `DESCRIPTOR_NOT_CREATED`, `BACKEND_NOT_CONFIGURED`, `CONNECTION_TEST_NOT_REAL_NETWORK`, `DEMO_UTXO_ID_*`, and `PSBT_NOT_CREATED`.

### Provider Operation Authorization Boundary

`SkaldVaultV1ProviderOperationAuthorizationPolicy` now exists as a still-disabled common model only. It models future provider operation kinds, operation purposes, required authorization gates, blockers, warnings, and disabled capabilities, but the current decision is always blocked/fail-closed. It accepts typed policy evidence only and does not accept provider handles, passphrases, keys, entropy, salts, nonces, ciphertext, plaintext, records, storage bytes, paths, Settings values, or platform handles.

The boundary does not run provider operations, provider KATs, runtime randomness checks, entropy/salt/nonce/key generation, Argon2id/KDF/HKDF/HMAC, AEAD encrypt/decrypt, header commitment computation or verification, record encrypt/decrypt, manifest or storage-index authentication, key wrapping/unwrapping, provider clear/dispose, vault creation, vault unlock, vault persistence, provider selection, or mainnet. Provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, OS keyrings and password managers remain rejected for Skald-managed vault passphrase storage, and passphrase-first remains the default authority.

### Runtime Randomness Authorization Boundary

`SkaldVaultV1RuntimeRandomnessAuthorizationPolicy` now exists as a still-disabled common model only. It models future randomness operation kinds, purposes, source kinds, required gates, blockers, warnings, and disabled capabilities for OS CSPRNG, reviewed provider randomness, hardware-backed entropy evidence, salts, nonces, key-generation input, KDF salts, AEAD nonces, test-vector scope, production runtime scope, release validation, Android entropy evidence, Linux entropy evidence, and mainnet randomness.

The current decision is always blocked/fail-closed. The boundary accepts typed policy evidence only and does not accept random bytes, entropy bytes, salt bytes, nonce bytes, key bytes, seed bytes, passphrases, provider handles, ciphertext, plaintext, record bytes, storage bytes, paths, Settings values, or platform handles. It does not call `SecureRandom`, Kotlin Random, Java Random, `Math.random`, OS randomness APIs, or provider randomness APIs. It does not run randomness health checks, generate entropy, generate salts, generate nonces, generate keys, create random byte fixtures, authorize deterministic test-vector randomness for production runtime, run provider operations, run KATs, run KDF/HKDF/HMAC/AEAD, make a provider selectable, enable vault creation, enable vault unlock, enable vault persistence, or approve mainnet.

Future acceptable randomness for secrets, salts, nonces, and keys must come from reviewed OS cryptographic randomness/CSPRNG or reviewed provider randomness. General-purpose PRNGs are forbidden for vault material. Android OS CSPRNG/SecureRandom remains future-reviewed only, Android hardware-backed key protection is separate from entropy quality, and Linux entropy quality remains a required review gate. Provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, OS keyrings and password managers remain rejected for Skald-managed vault passphrase storage, and passphrase-first remains the default authority.

### KDF Calibration Authorization Boundary

`SkaldVaultV1KdfCalibrationAuthorizationPolicy` now exists as a still-disabled common model only. It models future KDF operation kinds, purposes, parameter/evidence kinds, platform/device classes, required gates, blockers, warnings, disabled capabilities, and redacted policy tokens for Argon2id calibration finalization and KDF execution authorization.

Current KDF calibration authorization is blocked/fail-closed because provider operations are unauthorized, runtime randomness is unauthorized, passphrase input remains blocked, final KDF calibration is not approved, `productionProviderSelectable` remains false, and no production KDF execution is allowed. The boundary does not run Argon2id, run KDFs, run calibration, benchmark devices, inspect host/device details, approve final KDF parameters, normalize or encode real passphrases, generate or consume salts, call randomness APIs, run provider operations, run provider KATs, derive vault keys, enable vault creation, enable vault unlock, enable vault persistence, approve production provider use, or approve mainnet.

Android calibration remains future-reviewed only. Linux calibration remains future-reviewed only. Test-vector profiles do not authorize production runtime unlock, mainnet KDF use remains blocked until release review, provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, OS keyrings and password managers remain rejected for Skald-managed vault passphrase storage, and passphrase-first remains the default authority.

### Secure-Storage Authorization Boundary

`SkaldVaultV1SecureStorageAuthorizationPolicy` now exists as a still-disabled common model only. It models future secure-storage operation kinds, sensitive value kinds, storage target kinds, required gates, blockers, warnings, disabled capabilities, and redacted policy tokens for storing, retrieving, deleting, wrapping, unwrapping, exporting, importing, migrating, purging, or disclosing sensitive vault material.

Current secure-storage authorization is blocked/fail-closed because persistence readiness is blocked, secure secret storage and secure metadata storage are unavailable, provider operations are unauthorized, runtime randomness authorization is blocked, KDF calibration authorization is blocked, and no target is approved for production secret storage. The boundary does not store, retrieve, or delete secrets; wrap or unwrap keys; store wrapped keys; store metadata; export or import backup material; migrate or purge secure storage; use Android Keystore, Android Credential Manager, OS keyrings, password managers, SharedPreferences, Settings storage, files, or databases; read or write the encrypted local vault; run provider operations; run KDF/HKDF/HMAC/AEAD; call randomness APIs; enable vault creation, unlock, persistence, production provider use, or mainnet.

The app-controlled encrypted local vault remains the primary future storage model. OS keyrings remain rejected as primary storage and for Skald-managed vault passphrase storage, password managers remain rejected for Skald-managed vault passphrase storage, Settings/preferences remain rejected for secrets and sensitive metadata, and plaintext storage/logs/crash reports/analytics/support exports remain rejected for raw secret material. Android hardware-backed wrapping and Linux optional key wrapping remain future-reviewed only. Provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, and passphrase-first remains the default authority.

### Vault Unlock Authorization Boundary

`SkaldVaultV1UnlockAuthorizationPolicy` now exists as a still-disabled common model only. It models future unlock operation kinds, purposes, credential classes, required gates, blockers, warnings, disabled capabilities, and redacted policy tokens before any future branch can accept a passphrase, run KDF work, request randomness, call provider crypto, read secure storage, read encrypted vault storage, create decrypted session material, or transition a vault session to active.

Current unlock authorization is blocked/fail-closed because passphrase input is blocked, KDF calibration authorization is blocked, runtime randomness authorization is blocked, provider operations are unauthorized, secure-storage authorization is blocked, the storage service facade is disabled, lock/session lifecycle is unavailable, persistence readiness is blocked, and provider selection still selects only `DisabledVaultCryptoProvider`. The boundary does not accept passphrases, PINs, or biometrics; normalize or encode passphrases; run Argon2id/KDF/HKDF/HMAC/AEAD; generate or consume salts, nonces, or random bytes; call provider operations; read secure storage, metadata storage, or encrypted vault storage; unwrap keys; decrypt records; create active sessions; hold decrypted key material; persist unlock state; add UI; enable vault creation, unlock, persistence, production provider use, or mainnet.

OS keyrings remain rejected as primary storage and for Skald-managed vault passphrase storage. Password managers remain rejected for Skald-managed vault passphrase storage. Settings/preferences storage remains rejected for secrets and unlock state. Android unlock and hardware-wrapper evidence, Linux unlock and optional-wrapper evidence, and mainnet unlock remain future-reviewed only. Provider selection still selects only `DisabledVaultCryptoProvider`, `productionProviderSelectable` remains false, and passphrase-first remains the default authority.
